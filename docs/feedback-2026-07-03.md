# フィードバック対応(2026-07-03 実機確認)

実機確認で挙がった修正・追加要件の管理ドキュメント。グループ単位で実装を進め、完了したら ✅ を付ける。
参照キャプチャ: 筋トレMEMO の入力画面(前回記録パネル・セット個別コピー ↺・RM 列・メモ欄・キーボード上の「自重」ボタン)と履歴画面(日付カード・TOTAL/MAX 表示)。

## G1: レイアウトバグ修正(最優先)✅

- [x] 横幅がはみ出し、セット行右端の ✕(削除)が画面外に出る → セット行を画面幅に収まる構成に(G2 の行デザイン刷新と同時に恒久対応、まず暫定修正)
- [x] 縦幅が画面より少し小さく、下部メニューバーが浮いて見える → アプリルートを画面四辺に固定(fixed inset-0)する方式へ変更

## G2: セット行の刷新 ✅

筋トレMEMO の行構成を参考に、1 セット = 2 段(数値段 + メモ段)へ。

- [x] 目標レップ数を追加(実績レップ数と併記。目標は任意入力)
- [x] 各セットに推定 1RM を表示(実績重量×実績回数から Epley 式で自動計算、グレー表示)
- [x] 各セットにコメント欄(自由テキスト、プレースホルダ「メモ」)
- [x] 前セット(同日同種目ブロック内)からの個別コピーボタン: 重量 ↺ / レップ数(目標・実績)↺ / コメント ↺。アイコンのみ・説明文なし
- [x] 「自重」ボタン: 重量欄の脇に置き、タップで設定に登録した体重を重量欄へ入力(数字キーボードを保つため入力欄とは別のボタン)。入力後は ± で加減可(加重ディップス等)
- [x] 自重セットも通常の重量として 1RM 計算され、加重セットと同じ土俵で比較できる
- [x] 「補助」トグルは削除(G3 のセット属性クイックボタンに置き換え)

データモデル変更: `sets` に `targetReps?` を追加(memo は既存)。設定に `bodyWeight` を追加。Dexie version(2)。

## G3: 属性システム(セット属性・種目タグ共通の仕組み)✅

- [x] セット属性: 各セットに任意テキストの属性を付与できる
- [x] 属性バンク: 一度入力した属性は一覧から選択できる。件数が多い場合はインクリメンタルサーチで絞り込み
- [x] クイックボタン 3 つ: よく使う属性を即時付与。内容は設定画面で入れ替え可能
- [x] 種目タグ(高重量日など)にも同じ仕組みを適用: 任意テキスト・バンク・インクリメンタルサーチ・クイック 3 ボタン(設定で変更可)
  - 現状も新規タグの作成は可能だが、バンク/検索/クイックボタンの UX に刷新する

データモデル変更: セット属性のバンク用テーブル追加、`sets` に属性フィールド追加、設定にクイックボタン構成を追加。

## G4: 記録画面の情報・操作 ✅

- [x] 種目×タグごとに「前回の記録」パネルを表示(筋トレMEMO の Last Record 相当: 前回日付 + セット一覧)
- [x] 前回記録パネルから、さらに過去の記録を遡って閲覧できる(履歴へのリンク or その場でページング)
- [x] 日の記録内で種目ブロックの順番を入れ替えられる

## G5: デザイン全体刷新 ✅(第1弾: ダーク基調化・カード階調。細部の磨き込みは実機確認後に継続)

- [x] 全体的にスタイリッシュに(方向性はユーザーに確認して決定)

## 対応済み・確認事項(実装済みのため要動作確認)

- ✅ **前回コピーはタグまで考慮済み**: 「ベンチプレス×高重量日」の前回コピーは同じ組み合わせの直近記録だけをコピーする実装になっている(`copyPreviousSession` は複合インデックス `[exerciseId+tagId]` で検索)
- ✅ **日付まるごとの別日コピー/移動**: 履歴タブ → カレンダー → 日付を選択 → 「コピー/移動」から実行できる(M3 で実装済み)

## ユーザー確認済みの決定事項(2026-07-03)

- 自重: **設定画面に体重を 1 つ登録**。自重ボタンで重量欄に体重が入り、± で加重調整可。1RM は入った重量で通常計算
- セット属性: **1 セットに 1 つ**
- 並べ替え: **↑↓ボタン方式**
- デザイン: **ダーク基調で引き締める**(アクセント 1 色、数字が主役)

## 実施順序

G1(バグ)→ G2(行刷新)→ G3(属性)→ G4(前回記録・並べ替え)→ G5(デザイン)。
G2 と G3 はセット行の構成が絡むため、G2 の段階で属性表示スペースを想定した行レイアウトにしておく。
